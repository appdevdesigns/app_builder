dist: bionic

language: node_js

node_js:
    - 13

services:
    - docker

git:
    clone: false

before_install:
    - docker swarm init
    - git clone https://github.com/echu888/travis-appbuilder.git
    - cd travis-appbuilder

install:
    - scripts/installer.sh

script:
    - echo $HOME
    - pwd
    - npm run test
